# Interface for DHCP/DNS
interface=wlx00c0cab76a13

# DHCP range
dhcp-range=10.0.1.100,10.0.1.199,8h

# Gateway (router) option
dhcp-option=3,10.0.1.1

# DNS server offered to clients
dhcp-option=6,9.9.9.9

# Local test domains
address=/wstt.test/10.0.1.1
address=/watt.test/10.0.1.1
address=/wapt.test/10.0.1.1
address=/captive.portal/10.0.1.1

# Catch-all wildcard — resolves all unmatched domains to AP IP
# address=/#/10.0.1.1

# Forward unmatched DNS queries externally
server=9.9.9.9

# Optional for debug (log file is specified in service block)
# log-queries